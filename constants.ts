
// The default data has been moved to the `db.json` file, which serves as the
// file-based database for the Node.js server. The backend is now the single
// source of truth for the application's initial state.
